%h3 Users

- if @users.present?
  %table.table
    %thead
      %tr
        %th{scope: 'col'}
          Name
        %th{scope: 'col'}
          Phone number
        %th{scope: 'col'}
          Deployment
    %tbody
      - @users.each do |user|
        %tr
          %td= user.name
          %td= user.phone_number
          %td= user.deployment.city

- else
  There is no registered user at the moment.

